import { NavBar } from "./NavBar"
import "./Header.css"
import  Button from 'react-bootstrap/Button';

export const Header = () => {
  return (
    <header className="header">
      <NavBar/>
    </header>
  )
}
